# LLM provider: "mock" (deterministic, offline) or "deepseek" (real API)
LLM_PROVIDER=mock
DEEPSEEK_API_KEY=your_key_here
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# Canary traffic fraction for the canary model version (0.0 - 1.0)
CANARY_FRACTION=0.10
